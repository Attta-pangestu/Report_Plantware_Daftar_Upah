SELECT SUM(LFLN.Amount) AS TotalAmount
FROM "PR_LOOSEFRUIT_ARC" LF
JOIN "PR_LOOSEFRUITLN_ARC" LFLN
  ON LF.ID = LFLN.MasterID
WHERE LFLN.EmpCode LIKE 'H0510'
  AND LF.DocDate >= '2025-05-01'
  AND LF.DocDate < '2025-06-01'
